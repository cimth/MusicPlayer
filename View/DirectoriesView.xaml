<UserControl x:Class="View.DirectoriesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:View"
             xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="400">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </UserControl.Resources>
    
    <DockPanel>
        <!-- toolbar -->
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" 
                    Background="MintCream">
            <Button Content="Add" Margin="5" />
        </StackPanel>
        
        <!-- lists with directory contents -->
        <StackPanel Orientation="Vertical">
            <!-- sub directories -->
            <ListView ItemsSource="{Binding SubDirectoryPaths}"
                      SelectedItem="{Binding SelectedSubDirectoryPath}"
                      BorderThickness="0">
                <b:Interaction.Triggers>
                    <b:EventTrigger EventName="MouseDoubleClick">
                        <b:InvokeCommandAction Command="{Binding OpenSubDirectoryCommand}"/>
                    </b:EventTrigger>
                </b:Interaction.Triggers>
            </ListView>
            
            <!-- separator (only shown if sub directories AND music files exist) -->
            <Separator Visibility="{Binding HasSubDirectoriesAndMusicFiles, Converter={StaticResource BooleanToVisibilityConverter}}" />
        
            <!-- music files -->
            <ListView ItemsSource="{Binding MusicFilePaths}"
                      SelectedItem="{Binding SelectedMusicFilePath}"
                      SelectedIndex="{Binding SelectedMusicFileIndex}"
                      BorderThickness="0">
                <b:Interaction.Triggers>
                    <b:EventTrigger EventName="MouseDoubleClick">
                        <b:InvokeCommandAction Command="{Binding PlayAllSongsInDirectoryStartingWithTheSelectedSongCommand}"/>
                    </b:EventTrigger>
                </b:Interaction.Triggers>
            </ListView>
        </StackPanel>
    </DockPanel>
</UserControl>
